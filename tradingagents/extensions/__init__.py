"""Stable extension contracts for the team project.

Concrete implementations live in the sibling ``paper_trading``, ``memory``,
and ``decision`` packages.  Import shared types from ``contracts`` and public
ports from ``protocols`` so implementation packages remain replaceable.
"""
